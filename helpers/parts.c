#include "../includes/header_push_swap.h"

void tripple(Stack **a)
{
    if((*a)->n > (*a)->next->n &&
        (*a)->n < (*a)->next->next->n)
        sa(a, (*a)->next);
}
void quadruple(Stack **a, Stack **b)
{
    return ;
}
void quintuple(Stack **a, Stack **b)
{
    return ;
}

void centuple(Stack **a, Stack **b, t_important *data)
{
    Stack *tmp_a;

    tmp_a = *a;

    return ;
}
void fentuple(Stack **a, Stack **b, t_important *data)
{
    return ;
}